<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.technovation.sagetech.minder">

    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="ExtraText">

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->

        <activity android:name=".Intro" />
        <activity android:name=".quizzez.TestWhatIsInPhoto.WhereIsUsed" />
        <activity android:name=".IntroductionMap" />
        <activity android:name=".quizzez.TestWhatIsInPhoto.WhatIsInPhoto" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".quizzez.TestWhoIsInPhoto.WhoIsInPhoto" />
        <activity android:name=".quizzez.TestTrueFalse.Test1_TrueFalse" />
        <activity android:name=".quizzez.TestWordsFittingPhotos.WordFittingPhoto" />
        <activity android:name=".recycler_photo.ShowPhotoActivity" />
        <activity android:name=".authentication.StoreUserData" />
        <activity android:name=".MainActivity" />
        <activity android:name=".authentication.SignUpActivity" />
        <activity android:name=".authentication.LoginActivity" />
        <activity android:name=".LogoScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        activity android:name="info.androidhive.introslider.WelcomeActivity">

    </application>




</manifest>